# Database connection
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/rest_api"

# Server configuration
PORT=3000
NODE_ENV=development

# JWT Secret (for optional JWT authentication)
JWT_SECRET=your_jwt_secret_key